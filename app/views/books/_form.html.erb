<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
      <%= f.label :BookType %><br>
      <%= f.select :book_type_id, @book_types.map { |book| [book.name, book.id ] } %>
    </div>

    <div class="field">
      <%= f.label :status %><br>
      <%= f.select :status_id, @statuses.map { |status| [status.name, status.id ] } %>
    </div>


  <div class="field">
    <%= f.label :barcode %><br>
    <%= f.text_field :barcode %>
  </div>
  <div class="field">
    <%= f.label :INNcode %><br>
    <%= f.text_field :INNcode %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
